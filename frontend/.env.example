# Frontend Environment Variables
# React requires variables to start with REACT_APP_

# API URL - Leave as /api for development (uses proxy)
# For production, set to your backend URL
REACT_APP_API_URL=/api
