# ========================================
# SnapieVote - Port Configuration
# ========================================
# Copy this file to .env and customize for your VPS
# These are the EXTERNAL ports users will access
# (Internal Docker ports stay the same)

# Backend API - External port on your VPS
# Users/Frontend will access: http://your-vps-ip:BACKEND_PORT
# Default: 5000, change if taken (e.g., 5001, 5050, 8080)
BACKEND_PORT=5000

# Frontend UI - External port on your VPS  
# Users will access: http://your-vps-ip:FRONTEND_PORT
# Default: 3000, change if taken (e.g., 3001, 8000, 8080)
FRONTEND_PORT=3000

# ========================================
# Example for VPS with ports already in use:
# BACKEND_PORT=5050
# FRONTEND_PORT=8080
# Then access at: http://your-vps-ip:8080
# ========================================
